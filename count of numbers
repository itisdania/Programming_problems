/* number of numbers*/

int main() 
{ 
char z [1000];
int count = 0;
printf("enter a string: ");
fgets(z,sizeof(z),stdin);

for(int i = 0; z[i] != '\0'; ++i) 
{ 
if(  z[i]>='0' && z[i]<='9')
        ++count ;
 }
 printf("count %d\n",count);
  
 return 0; 
 }
